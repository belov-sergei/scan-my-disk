CREATE_PROJECT(MakeFont
	SOURCES
		"${PROJECT_SOURCE_DIR}/Tools/MakeFont"
	SOURCE_GROUP
		"${PROJECT_SOURCE_DIR}"
	WORKING_DIRECTORY
		"${PROJECT_SOURCE_DIR}"
	LINK_LIBRARIES
		Kernel
	FOLDER
		"Tools"
)

CREATE_PROJECT(MakeText
	SOURCES
		"${PROJECT_SOURCE_DIR}/Tools/MakeText"
	SOURCE_GROUP
		"${PROJECT_SOURCE_DIR}"
	WORKING_DIRECTORY
		"${PROJECT_SOURCE_DIR}"
	LINK_LIBRARIES
		Kernel
	FOLDER
		"Tools"
)

CREATE_PROJECT(MakeIcons
	SOURCES
		"${PROJECT_SOURCE_DIR}/Tools/MakeIcons"
	SOURCE_GROUP
		"${PROJECT_SOURCE_DIR}"
	WORKING_DIRECTORY
		"${PROJECT_SOURCE_DIR}"
	LINK_LIBRARIES
		Kernel
	FOLDER
		"Tools"
)